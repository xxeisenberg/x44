# URL for the control-plane API backend
NEXT_PUBLIC_CONTROL_PANEL_URL="http://localhost:8787"

# URL for the build-worker daemon (used for streaming build logs via SSE)
NEXT_PUBLIC_BUILD_WORKER_URL="http://localhost:8080"

# Public URL of the frontend web dashboard (used for GitHub OAuth redirect callback)
NEXT_PUBLIC_SITE_URL="http://localhost:1844"
